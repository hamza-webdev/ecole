{% extends 'base.html.twig' %}

{% block title %}TypeExamin{% endblock %}

{% block body %}
    <h1>TypeExamin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_examin.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ type_examin.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_examin_index') }}">back to list</a>

    <a href="{{ path('type_examin_edit', {'id': type_examin.id}) }}">edit</a>

    {{ include('type_examin/_delete_form.html.twig') }}
{% endblock %}
