{% extends 'base.html.twig' %}

{% block title %}Civilite{% endblock %}

{% block body %}
    <h1>Civilite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ civilite.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ civilite.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('civilite_index') }}">back to list</a>

    <a href="{{ path('civilite_edit', {'id': civilite.id}) }}">edit</a>

    {{ include('civilite/_delete_form.html.twig') }}
{% endblock %}
