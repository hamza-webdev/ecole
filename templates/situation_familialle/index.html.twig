{% extends 'base.html.twig' %}

{% block title %}SituationFamilialle index{% endblock %}

{% block body %}
    <h1>SituationFamilialle index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Classeroom</th>
                <th>Desciption</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for situation_familialle in situation_familialles %}
            <tr>
                <td>{{ situation_familialle.id }}</td>
                <td>{{ situation_familialle.title }}</td>
                <td>{{ situation_familialle.Classeroom }}</td>
                <td>{{ situation_familialle.desciption }}</td>
                <td>
                    <a href="{{ path('situation_familialle_show', {'id': situation_familialle.id}) }}">show</a>
                    <a href="{{ path('situation_familialle_edit', {'id': situation_familialle.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('situation_familialle_new') }}">Create new</a>
{% endblock %}
