<form method="post" action="{{ path('situation_familialle_delete', {'id': situation_familialle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ situation_familialle.id) }}">
    <button class="btn">Delete</button>
</form>
