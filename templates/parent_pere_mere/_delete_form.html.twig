<form method="post" action="{{ path('parent_pere_mere_delete', {'id': parent_pere_mere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parent_pere_mere.id) }}">
    <button class="btn"  class="btn btn-danger">Delete</button>
</form>
