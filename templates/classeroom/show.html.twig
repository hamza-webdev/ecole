{% extends 'base.html.twig' %}

{% block title %}Classeroom{% endblock %}

{% block body %}
    <h1>Classeroom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classeroom.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ classeroom.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ classeroom.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('classeroom_index') }}">back to list</a>

    <a href="{{ path('classeroom_edit', {'id': classeroom.id}) }}">edit</a>

    {{ include('classeroom/_delete_form.html.twig') }}
{% endblock %}
