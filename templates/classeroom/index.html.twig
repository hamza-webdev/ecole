{% extends 'base.html.twig' %}

{% block title %}Classeroom index{% endblock %}

{% block body %}
    <h1>Classeroom index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for classeroom in classerooms %}
            <tr>
                <td>{{ classeroom.id }}</td>
                <td>{{ classeroom.title }}</td>
                <td>{{ classeroom.description }}</td>
                <td>
                    <a href="{{ path('classeroom_show', {'id': classeroom.id}) }}">show</a>
                    <a href="{{ path('classeroom_edit', {'id': classeroom.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('classeroom_new') }}">Create new</a>
{% endblock %}
